#include <iostream>
using namespace std; // чтобы не указывать префикс имен функций std.

int main() {
  int a = 0;
  int b = 0;

  cout << "Enter a and b: ";
  cin >> a >> b;

  cout << "a + b = " << (a + b) << endl; 

  return 0;
}
